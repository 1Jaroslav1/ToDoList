operating-mode: normal
server:
     error:
          include-message: always
          include-binding-errors: always
spring:
     datasource:
          password: Atomuran92
          url: jdbc:mysql://localhost:3306/conection?useSSL=false
          username: root
     jpa:
          hibernate:
               ddl-auto: update
          properties:
               hibernate:
                    dialect: org.hibernate.dialect.MySQL5InnoDBDialect
                    format_sql: true
          show-sql: true
          defer-datasource-initialization: true
     sql:
          init:
               mode: never
     servlet:
          multipart:
               max-file-size: 200KB
               max-request-size: 200KB
     mail:
          host: smtp.gmail.com
          port: 587
          username: crmpap111@gmail.com
          password: sfmfidcscqondtjt
          properties:
               mail:
                    smtp:
                         ssl:
                              trust: '*'
                         auth: true
                         starttls:
                              enable: true
                         connectiontimeout: 5000
                         timeout: 3000
                         writetimeout: 5000
